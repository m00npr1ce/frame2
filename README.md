# Система управления задачами для строительной компании

Микросервисная архитектура с тремя основными компонентами: API Gateway, Users Service и Orders Service.

## 📋 Содержание

- [Архитектура](#архитектура)
- [Технологический стек](#технологический-стек)
- [Быстрый старт](#быстрый-старт)
- [API Документация](#api-документация)
- [Структура проекта](#структура-проекта)
- [Конфигурация](#конфигурация)
- [Безопасность](#безопасность)
- [Тестирование](#тестирование)

## 🏗️ Архитектура

Система состоит из трех основных сервисов:

1. **API Gateway** (порт 3000) - единая точка входа
   - Проксирование запросов к микросервисам
   - JWT валидация
   - Rate limiting
   - CORS настройка
   - Корреляция запросов по заголовку `X-Request-ID` и логирование

2. **Users Service** (порт 3001) - управление пользователями
   - Регистрация и аутентификация
   - Управление профилями
   - Ролевая модель доступа

3. **Orders Service** (порт 3002) - управление заказами
   - CRUD операции для заказов
   - Управление статусами заказов
   - Пагинация и сортировка

## 🛠️ Технологический стек

- **Node.js 20** - среда выполнения
- **Express.js** - веб-фреймворк
- **PostgreSQL** - база данных
- **Docker & Docker Compose** - контейнеризация
- **JWT** - аутентификация
- **bcrypt** - хеширование паролей
- **Zod** - валидация данных
- **Pino** - логирование
- **OpenAPI** - документация API

## 🚀 Быстрый старт

### Предварительные требования

- Docker и Docker Compose
- Node.js 20+ (для локальной разработки)

### Запуск через Docker Compose

1. Клонируйте репозиторий или перейдите в директорию проекта

2. Запустите все сервисы:
```bash
docker-compose up --build
```

3. Система будет доступна по адресу:
   - API Gateway: http://localhost:3000
   - Users Service: http://localhost:3001
   - Orders Service: http://localhost:3002
   - Swagger UI: http://localhost:3000/api-docs

### Локальная разработка

1. Установите зависимости для каждого сервиса:
```bash
cd api_gateway && npm install
cd ../service_users && npm install
cd ../service_orders && npm install
```

2. Запустите PostgreSQL базы данных:
```bash
docker-compose up postgres-users postgres-orders
```

3. Создайте `.env` файлы для каждого сервиса (см. раздел Конфигурация)

4. Запустите сервисы:
```bash
# Terminal 1
cd api_gateway && npm run dev

# Terminal 2
cd service_users && npm run dev

# Terminal 3
cd service_orders && npm run dev
```

## 📚 API Документация

OpenAPI спецификация находится в `docs/openapi.yaml`, а живая документация доступна через Swagger UI по адресу `http://localhost:3000/api-docs`.

### Основные эндпоинты

#### Аутентификация

- `POST /users/v1/register` - Регистрация
- `POST /users/v1/login` - Вход

#### Пользователи

- `GET /users/v1/profile` - Получить профиль (требует JWT)
- `PUT /users/v1/profile` - Обновить профиль (требует JWT)
- `GET /users/v1/users` - Список пользователей (только админы)

#### Заказы

- `POST /orders/v1/orders` - Создать заказ (требует JWT)
- `GET /orders/v1/orders` - Список заказов (требует JWT)
- `GET /orders/v1/orders/:id` - Получить заказ (требует JWT)
- `PUT /orders/v1/orders/:id/status` - Обновить статус (требует JWT)
- `DELETE /orders/v1/orders/:id` - Удалить заказ (требует JWT)

### Примеры запросов

#### Регистрация
```bash
curl -X POST http://localhost:3000/users/v1/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "password123",
    "name": "John Doe"
  }'
```

#### Вход
```bash
curl -X POST http://localhost:3000/users/v1/login \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "password123"
  }'
```

#### Создание заказа
```bash
curl -X POST http://localhost:3000/orders/v1/orders \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -d '{
    "items": [
      {
        "product": "Concrete Mix",
        "quantity": 10
      }
    ]
  }'
```

### Postman коллекция

В репозитории уже подготовлены коллекция и окружение для локальной разработки:

- `Construction_Company_API.postman_collection.json` — набор запросов для всего API (регистрация, логин, профиль, CRUD заказов, админские операции).
- `Local_Development.postman_environment.json` — окружение с переменной `baseUrl = http://localhost:3000`.

Для проверки работы через Postman:
- Импортируйте коллекцию и окружение.
- Выберите окружение **Local Development**.
- Выполните запросы из разделов **Authentication**, затем **Users** и **Orders** — скрипты коллекции автоматически подставляют токен и идентификаторы.

## 📁 Структура проекта

```
project/
├── api_gateway/
│   ├── Dockerfile
│   ├── package.json
│   └── src/
│       └── index.js
├── service_users/
│   ├── Dockerfile
│   ├── package.json
│   └── src/
│       ├── index.js
│       ├── db.js
│       ├── controllers/
│       ├── routes/
│       ├── middleware/
│       └── validators/
├── service_orders/
│   ├── Dockerfile
│   ├── package.json
│   └── src/
│       ├── index.js
│       ├── db.js
│       ├── controllers/
│       ├── routes/
│       ├── middleware/
│       └── validators/
├── docs/
│   └── openapi.yaml
├── docker-compose.yml
└── README.md
```

## ⚙️ Конфигурация

### Переменные окружения

#### API Gateway
- `PORT` - порт сервера (по умолчанию: 3000)
- `USERS_SERVICE_URL` - URL сервиса пользователей
- `ORDERS_SERVICE_URL` - URL сервиса заказов
- `JWT_SECRET` - секретный ключ для JWT
- `CORS_ORIGIN` - разрешенные источники для CORS
- `LOG_LEVEL` - уровень логирования

#### Users Service
- `PORT` - порт сервера (по умолчанию: 3001)
- `DB_HOST` - хост базы данных
- `DB_PORT` - порт базы данных
- `DB_USER` - пользователь базы данных
- `DB_PASSWORD` - пароль базы данных
- `DB_NAME` - имя базы данных
- `JWT_SECRET` - секретный ключ для JWT
- `JWT_EXPIRES_IN` - время жизни токена (по умолчанию: 24h)
- `LOG_LEVEL` - уровень логирования

#### Orders Service
- `PORT` - порт сервера (по умолчанию: 3002)
- `DB_HOST` - хост базы данных
- `DB_PORT` - порт базы данных
- `DB_USER` - пользователь базы данных
- `DB_PASSWORD` - пароль базы данных
- `DB_NAME` - имя базы данных
- `JWT_SECRET` - секретный ключ для JWT
- `LOG_LEVEL` - уровень логирования

## 🔐 Безопасность

- **JWT аутентификация** - все защищенные маршруты требуют валидный JWT токен
- **Хеширование паролей** - пароли хешируются с помощью bcrypt (10 раундов)
- **Ролевая модель доступа** - поддержка ролей 'user' и 'admin'
- **Rate limiting** - ограничение частоты запросов (100 запросов за 15 минут)
- **Валидация данных** - все входные данные валидируются с помощью Zod
- **CORS** - настраиваемая политика CORS

### Создание администратора

Для создания пользователя с правами администратора, выполните SQL запрос:

```sql
UPDATE users SET roles = ARRAY['user', 'admin']::TEXT[] WHERE email = 'admin@example.com';
```

## 🧪 Тестирование

### Unit‑тесты

Для сервисов пользователей и заказов есть unit‑тесты контроллеров (Jest):

```bash
cd service_users && npm test
cd ../service_orders && npm test
```

### Ручное тестирование

Используйте Postman, curl или любой другой HTTP клиент для тестирования API.

### Пример тестового сценария

1. Зарегистрируйте пользователя.
2. Войдите в систему и получите JWT токен.
3. Используйте токен для создания заказа.
4. Получите список своих заказов.
5. Обновите статус заказа.

## 📝 Формат ответов

Все ответы API следуют единому формату:

### Успешный ответ
```json
{
  "success": true,
  "data": {
    // данные ответа
  }
}
```

### Ошибка
```json
{
  "success": false,
  "error": {
    "code": "ERROR_CODE",
    "message": "Описание ошибки"
  }
}
```

## 🔍 Логирование

Все сервисы используют Pino для логирования. Логи включают:
- Request ID для трассировки запросов
- Временные метки
- Уровни логирования (info, error, warn, debug)

## 🐳 Docker

### Сборка образов

```bash
docker-compose build
```

### Просмотр логов

```bash
docker-compose logs -f [service_name]
```

### Остановка сервисов

```bash
docker-compose down
```

### Остановка с удалением volumes

```bash
docker-compose down -v
```

## 📄 Лицензия

ISC

## 👥 Автор

Рудаков Иван ЭФБО-03-23

Проект выполнен в рамках практической работы по фреймворкам.


